export component Inventory {
    width: 100%;
    height: 100%;
    HorizontalLayout {
        alignment: start;
        VerticalLayout {
            alignment: start;
            Text {
                text: "e";
            }
        }
        Text {
            stroke-style: TextStrokeStyle.center;
            text: "Inventory";
            color: black;
        }
    }
}
